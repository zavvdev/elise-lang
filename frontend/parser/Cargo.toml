[package]
name = "elise-parser"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
elise-errors = { path = "../../shared/errors" }
elise-types = { path = "../../shared/types" }
elise-ast = { path = "../ast" }
regex.workspace = true
