[package]
name = "elise-semanalyzer"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
elise-shared = { path = "../../shared" }
elise-ast = { path = "../ast" }
elise-parser = { path = "../parser" }

[dev-dependencies]
elise-test-utils = { path = "../../test-utils" }
