pub mod ast;

fn main() {
    //    let re = Regex::new(r"(-?[0-9]\d*(\.\d+)?)").unwrap();
    //
    //    let input = "-2, -1.45, 0, 1, 2.34, 99";
    //
    //    re.find_iter(input).for_each(|c| {
    //        println!("{:#?}", c);
    //    });
}
